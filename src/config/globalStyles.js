import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  },
  pageTitle: {
    position: 'absolute',
    top: '5%',
    fontSize: 22,
    fontWeight: 'bold',
    color: '#7d7d7d',
  },
});
